# pnpm 配置。
# 不再指定 store-dir / cache-dir：使用各平台默认值
# （Linux: ~/.local/share/pnpm，Windows: %LOCALAPPDATA%\pnpm）。
# 注意：原先的 Windows 绝对路径（D:/Code/dsh/plugins/.pnpm-store）
# 在 Linux 上不可用，会导致 pnpm 静默回退，请勿恢复绝对路径写法。

| `user-profile` | 无文件可扫，走**引导式问答**                                 | 沟通语言、回复风格、身份、偏好            | 全局库按钮做成问答，不扫文件 |
| -------------- | ------------------------------------------------------------ | ----------------------------------------- | ---------------------------- |
| `environment`  | `~/.dsh/` 配置、`/etc/os-release`、`nvidia-smi` 输出、`docker ps` 输出、`~/.bashrc` 别名 | 服务器配置、GPU、网络、已装工具、端口占用 | 可跑只读命令采集             |
| `conventions`  | `~/.dsh/` 全局配置、跨项目 AGENTS.md 的公共部分              | 通用技术选型、编码规范、工具偏好          | 低频，用户主动告知为主       |